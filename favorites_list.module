<?php

/*
 *Implementing hook_theme().
 */

function favorites_list_theme($existing, $type, $theme, $path) {

	return array(
		'favorites_list' => array(
			'variables' => array('items' => array(), 'title' => '', 'url' => '')
		)
	);
}
